mainClassName = "jdblender.jdbc.AppJdbc"

dependencies {
    compile project(':core')
}

jar {
manifest {
    attributes(
      //'Class-Path': configurations.compile.collect { it.getName() }.join(' '),
      'Main-Class': "jdblender.jdbc.AppJdbc"
    )
  }
}