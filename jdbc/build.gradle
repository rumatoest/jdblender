apply plugin: 'application'
mainClassName = "dbshaker.jdbc.AppJdbc"

applicationDefaultJvmArgs = ["-XX:CompileThreshold=100"]

dependencies {
    compile project(':core')
}

run {
    args = ["${projectDir}/report-${project.name}.csv",]
}